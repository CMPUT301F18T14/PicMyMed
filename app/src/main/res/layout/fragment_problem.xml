<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.picmymedcode.View.ProblemFragment">

    <!-- TODO: Update blank fragment layout -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <SearchView
            android:id="@+id/searchProblem"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:queryHint="@string/searchKeyword"
            android:background="@drawable/blue_gradient"
            android:layout_below="@+id/Tab_Search_Toolbar"
            android:iconifiedByDefault="false"></SearchView>
        <Button
            android:id="@+id/problem_search_location_button"
            android:layout_width="350dp"
            android:layout_height="wrap_content"
            android:layout_below="@+id/searchProblem"
            android:layout_marginTop="5dp"
            android:layout_centerHorizontal="true"
            android:background="@drawable/signupbtn1"
            android:text="Search by location" />
        <Button
            android:id="@+id/problem_search_bodylocation_button"
            android:layout_width="350dp"
            android:layout_height="wrap_content"
            android:layout_below="@+id/problem_search_location_button"
            android:layout_marginTop="5dp"
            android:layout_centerHorizontal="true"
            android:background="@drawable/signupbtn1"
            android:text="Search by bodylocation" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/fragment_problem_recycle_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/problem_search_bodylocation_button"
            android:scrollbars="vertical" />
    </RelativeLayout>
</FrameLayout>